spring:
  application:
    name: ssuai
  profiles:
    default: dev

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never

